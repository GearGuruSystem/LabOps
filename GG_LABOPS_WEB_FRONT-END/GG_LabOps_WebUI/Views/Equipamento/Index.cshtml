@model IEnumerable<EquipamentosModel>

@{
    ViewBag.Title = "Equipamentos";
}

<h4>Equipamentos</h4>
<br />
<div class="mb-3">
    <input type="text" id="searchInput" class="form-control" placeholder="Buscar...">
</div>
<div class="table-responsive">
    <table id="tabela-Equipamentos" class="table table-hover table-borderless caption-top">
        <caption>Lista de Equipamentos</caption>
        <thead>
            <tr>
                <th>#</th>
                <th>NOME</th>
                <th>INVENTARIO</th>
                <th>HOSTNAME</th>
                <th>SITUAÇÃO</th>
                <th>TIPO DE EQUIPAMENTO</th>
                <th>FABRICANTE</th>
            </tr>
        </thead>
        <tbody class="table-group-divider-top-color">
            @foreach (var equipamento in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(e => equipamento.Id)</td>
                    <td>@Html.DisplayFor(e => equipamento.Nome)</td>
                    <td>@Html.DisplayFor(e => equipamento.Inventario)</td>
                    <td>@Html.DisplayFor(e => equipamento.Hostname)</td>
                    <td>@Html.DisplayFor(e => equipamento.FabricanteNome)</td>
                    <td>@Html.DisplayFor(e => equipamento.TipoEquipamentoDescricao)</td>
                    <td>@Html.DisplayFor(e => equipamento.SituacaoDescricao)</td>
                    <td>
                        <a asp-action="Editar" class="btn btn-info btn-sm" asp-route-id="@equipamento.Id">Editar</a>
                        <a asp-action="Deletar" class="btn btn-danger btn-sm" asp-route-id="@equipamento.Id">Deletar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
<div id="pagination" class="mt-3"></div>